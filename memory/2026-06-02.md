# 2026-06-02

## CRM Updates
- Patched `app/contacts/contacts-table.tsx` to persist `currentPage` in `sessionStorage` so navigating back from a contact detail page doesn't reset the list to page 1.
- Addressed issue where CRM only showed 10,000 contacts due to Supabase API Max Rows limits.
- Refactored `CRM` contacts list to use server-side pagination (`.range(from, to)`) instead of fetching all records at once and slicing client-side. This ensures the app can scale well beyond 10,000 contacts efficiently. (Commit `14048b2`).
