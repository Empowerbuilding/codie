
## 2026-08-24 ~02:45 UTC — New Meta lead form routing (Michael)
- New form: "Qualifying Form EZZPZZ - High Intent" (SMS-verified), form_id `1379082134439027`, attached to 2 paused Split Vision ad drafts (Web & Form + Industrial Traffic ad sets).
- **n8n "Facebook form to CRM" (mzGGN4R1Cb95eSGD):** realtime trigger switched old EZZPZZ (1654336582197622) → new HI form. ⚠️ n8n limit: ONE Facebook Lead Ads trigger per Meta app (app 26277883501814236) — cannot listen to both forms realtime. Also added `design_investment` (new $3/sqft qualifier question) to CRM metadata payload.
- **n8n "Barnhaus - FB Leads Catch-Up Sync" (aDgVyUyNFEB0B9ZZ):** now polls BOTH forms hourly (new node "Fetch HI FB Leads (48hr)", chained sequentially), parse code tags per-lead form_id/form_name, POST metadata no longer hardcodes old form. Old-form leads now route via this hourly sweep (welcome email up to ~1hr delayed).
- No CRM code changes needed — webhook accepts metadata generically; design_investment auto-displays in contact notes.
- If realtime on BOTH forms is ever needed → requires second Meta app/credential (Mitch decision).

## Nightly log (03:00 UTC)
- Files modified since last nightly: render-tool Studio work shipped late on 08-23 (commits d4ea86f Studio tab v1 + f26c20e drag/drop/paste/URL input for plate & ref boxes, both deployed — details in 2026-08-23.md); RENDER-WORKFLOW-BUILD-SPEC.md updated with §5b concrete Studio screen layout (Michael 21:42 UTC) incl. decided branch-from-here rewind + slider-wipe before/after — committing tonight; new untracked media-out/studio-mockup.html + studio-tab-mockup.png (UI mockups).
- CRM repo files show fresh mtimes from a re-pull only — no new CRM commits since f2babda (08-18); no design-os activity.
- Early-morning n8n work (~02:45): new Meta HI lead form 1379082134439027 routed — realtime trigger switched in "Facebook form to CRM", catch-up sync now polls both forms hourly (logged above, committed as 377c6bd).
- In-flight: Studio v1 next iterations (vision QC grader, silent auto-checkpoints, Library→Studio handoff, color-lock anchor); open UI decision: silent vs toast on auto-checkpoint; test workflows rt-texture-apply-test / rt-segment-mask-test still active pending cleanup.

## Studio v1.2.6.1 — Michael's first real run + feedback round (04:30-05:00 UTC)
- Michael ran first real Studio session (maca, 9 steps off plate rihp9ct1deeb0kaztbpp): wainscot removal held through all composes; his step-9 "NO STONE WAINSCOT" caps worked (vision-verified chimney stone confined). His checkpoint 7 melted (orange/soft), checkpoint 8 recovered to plate parity DESPITE using melted ck7 as design ref — compose-from-plate self-heals from bad design reference (good finding).
- Shipped commit 2f95f9f (deployed, finished): history click = viewer modal w/ red "Branch from here" + warning (was: click = instant branch); paste image/URL into URL inputs; checkbox renamed "📎 Use in generations" + tooltip; reference prompt framing now label-driven/flexible (remodel-style refs work, not just 2D swatches); 🌿 Refresh landscaping quick chip (prefills vegetation-regen prompt).
- Michael confirmed flexible refs worked (pointed at "siding in reference image" remodel-style, got close).
- NEXT UP (Michael's asks, not yet built): first-class lot/site slot w/ vision site-spec pass (needs new n8n vision workflow — edit prompt framing fights lot photos); project save/load + save-to-library UX (note: approved steps ALREADY auto-save to Library as render_type=studio); QC grader for silent checkpoint re-rolls; color-lock anchor still open. He reported a UI glitch during his double-checkpoint moment — unreproduced, watch for it.

## Studio v1.2.6.2 (commit a3ec82b, deployed ~05:05 UTC)
- COLOR LOCK SHIPPED: checkpoint compose now anchors color grade/WB/sky/time-of-day to Image 1 (plate), NOT the approved design ref (which propagated drift — root cause of rainbow checkpoints). Exception clause: recipe items that explicitly change environment/lighting win. This was the long-open "color-lock anchor" item — solved in prompt.
- Anti-melt guardrail added to all edits: preserve full sharpness/texture/resolution, no softening of unmentioned surfaces/foliage.
- 🔍 Full prompt inspector: collapsible at bottom of stage shows exact last prompt sent; fullPrompt also now saved into renders.options for forensics.
- Verified for Michael: checkpoints ALWAYS compose from plate.url (1 gen from plate, 2 from raw upload); edits chain from workingUrl.
- Ops note: inline Coolify token got redacted/mangled mid-session ("Unauthenticated") — fixed by grepping token from AGENTS.md at runtime. Always read tokens from file.

## Studio v1.2.6.3 (commit ff65eff, deployed ~05:20 UTC)
- References now numbered (REF 1/2/3 badge on cards); prompts map "Image N = REFERENCE n: label" + tell model "reference N"/"ref N" in instructions refers to that mapping — users can say "use ref 2" in steps.
- CHECKPOINT REF FIX: approved steps now store refsUsed; checkpoint compose auto-carries the union of ALL refs used in any approved step (deduped) + currently-checked refs, regardless of checkbox state. Previously unchecked refs silently dropped from composes (recipe text said "in reference image" with no image attached).
- Explained to Michael: bimodal roll behavior (faithful-edit vs restyle mode) is model stochasticity — QC grader is the fix; color lock shrinks bad mode.
- Site/lot designation: still just a labeled ref for now; dedicated Site/Lot slot + vision site-spec pass still queued (needs new n8n vision workflow).
- Created workspace/deploy-render-tool.sh (reads Coolify token from AGENTS.md at runtime) — inline tokens kept getting mangled by redaction; use this script for all render-tool deploys.

## Studio v1.2.7.1 (commit 75a1fc0, deployed ~05:30 UTC) — night cap
- PROJECT SAVE/RESTORE: 💾 "Save project to Library" button (render_type='studio-project', full state in options.project: plate/refs/steps/workingUrl); "Start new project" now AUTO-SAVES current project first; 📂 "Open saved project" modal in Studio lists saved projects (thumb/date/recipe) → click restores full state. Library shows entries too (badge studio-project, latest iteration as image).
- Prompt inspector fix: fullPrompt now stored on each approved step + lastPrompt persisted in localStorage — inspector survives page refresh (was memory-only, why Michael lost it).
- Landscape refresh prompt harmonized with guardrails (explicitly scopes do-not-touch to house/hardscape/camera for that edit).
- Michael called it for the night. HIS REMAINING ASKS FOR NEXT SESSION: (1) dedicated Site/Lot slot w/ vision site-spec pass (top priority — needs new n8n vision workflow), (2) QC grader w/ silent checkpoint re-rolls, (3) Library-side "load to Studio" polish for studio-project entries, (4) Finalize button (fresh compose + HQ upscale), (5) "more fluidity" between quick actions and master guardrails. He says tool is a great starting point; steps work well as-is.

## Studio v1.2.7.2 (commit 91d9547, deployed ~05:35 UTC)
- Step viewer now has "💾 Save to Library" — saves that individual history image as a favorited renders row (render_type=studio, options.savedFromHistory=true).
