
## 2026-05-22
- Wired up drag-and-drop for the ARCHIVED column in barnhaus-design-os.
- Added canvas-confetti package and implemented a grayscale explosion when dropping a card into the archive.
- Fixed Chrome 3D card drag image ghosting by generating a 2D clone on drag start.
- Implemented optimistic UI state on drag-and-drop to remove API latency lag.
- Removed 'update client on timeline' hijack from the Weekly Ribbon.
- Added a dedicated 'Update client on timeline' UI to ProjectCard that glows when ticker <= 3 and allows manually resetting the ticker.
- Iterated on 'freeze' card state visuals; currently using a CSS-based cracked ice block with hard-light blending.
