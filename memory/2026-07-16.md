
- Deployed `v1.1.2.6` of Render Tool which refactors the image saving behavior. Instead of automatically saving all generations, renders are now completely ephemeral until the user clicks 'Save to Library'.
