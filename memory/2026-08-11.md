- **Render Tool:** Added "view angle" to project style presets and added customizable prompt bubbles beneath checked options in the enhancement options list. Now users can "x" out default prompt fragments or "+" add new words to inject dynamically into the final generated prompt. Deployment finished successfully.
- **Render Tool (Video Update):** Removed post-render video option and added a dedicated "Video (Animate)" tab. The UI provides input methods matching other tabs, allows toggling between single image and image-to-image (Start/End), and includes dropdown options for Camera Movement, Movement Speed, and Video Format (16:9, 9:16, 1:1, 5:4).
- **Render Tool (Video Fix):** Fixed a React crash (blank screen) on the new Video tab caused by a missing reference to the `handleVideoGenerate` function.
- **Render Tool (Video UI Update):** Replaced the generic upload boxes in the Video tab with the standardized custom upload UI component (with "Drag & Drop", URL input, paste support, preview containers) to match the other tool sections like Exterior and Remodel. 
- **Render Tool (Video Upload UI Final Fix):** Made sure the upload containers in the Video tab exactly match the custom component structure required (wrapping in `file-upload-group`, rendering the "or" divider correctly, and matching the exact `📸` UI layout). 
- **Render Tool (Video Drag and Drop Fix):** Modified the `onDrop` events in the Video tab to parse dataTransfer for both URLs (from drag and drop across the app, like the asset palette) and raw files (from OS filesystem), mirroring the robust input logic of the other tabs. 
