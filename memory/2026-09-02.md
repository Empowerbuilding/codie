# 2026-09-02

## Keyframe Assist: orbit now generates AI end frame (v1.2.10.1, `0372b6b`, deployed finished)
- Michael: wanted first+last frame approval for ALL camera motions. Reality before: zoom/pan/tilt = crop pairs (both approved), dolly = AI end frame, orbit = single frame (Veo-driven, no end frame). His memory of "orbit with final keyframe" was likely a crop/dolly move.
- Frontend `KeyframeAssist.jsx`: orbit-left/right type 'veo'→'ai'; orbit instructions added to buildKeyframePrompt (arc left/right, subject centered, MOVE_AMOUNTS.orbit = 15°/30°/45°/60° for subtle/medium/big/huge); removed all 'veo'-type branches/UI hints; buildVideoPrompt preview uses "curved orbital arc" phrasing when orbit+endFrame.
- n8n: `RT - Exterior Video` (BR34NolETCR4i0RG) Build Veo Request1 — start-end prompt now conditional: orbit → "curved orbital arc around the subject, keeping the subject centered" instead of "single straight path". `RT - Exterior Video Mobile` (GzFmqQvviTmwIUBS) was MISSING the start-end prompt rewrite entirely (always used directional phrasing) — brought in line w/ wide workflow incl. orbit branch. Both saved, still active.
- No change needed to `rt-keyframe-next` (uses fullPrompt verbatim).
- Watch: AI orbit end frames were originally avoided ("image models can't synthesize orbital view") — if Gemini end frames look bad at big/huge, dial degree language down or coach subtle/medium.

## Orbit end-frame prompt tuning (v1.2.10.2, deployed finished)
- Michael: 3 regens → end identical to start. Verified via n8n exec logs + vision compare: v1 orbit prompt cloned the frame ~2/3 rolls (sameness anchoring dominates).
- Iterated live against rt-keyframe-next w/ his exact frame (`home-designs/edits/cdqixuv8kb0fqopff2kp.jpg`):
  - v2 "circular arc" stronger wording: 1/3 real orbit.
  - v3 named TARGET COMPOSITION ("three-quarter view, corner nearest camera"): 3/3 viewpoint changes but 0/3 correct direction — "the building's right-hand side" is ambiguous (building-centric vs viewer-centric!).
  - v4 = v3 + VIEWER-relative direction ("camera moves toward the RIGHT EDGE of the current frame; whatever is on the right side of the image swings toward camera"): 2/3 correct direction, 1 clean pass, 1 overshoot, 1 clone.
- Shipped v4 language into buildKeyframePrompt (`orbit degrees now 15/35/50/70 for subtle/medium/big/huge; amt.orbit.split(' - ')[0]` embeds degree phrase). Commit `<v1.2.10.2>`. Model is still stochastic — regenerate 1-3x is the expected UX; QC-grader-style auto-reroll would fix properly.
- LESSON (generalizes to Studio too): image-model camera/direction instructions must be FRAME/VIEWER-relative and name the target composition, not the motion.
- Gotcha: render-tool Supabase anon+service keys in MEMORY.md returned 401 on REST today — possibly rotated/mangled; used n8n exec logs instead. Verify keys next session.

## Supabase anon key rotation (barnhaus-design-os local copy)
- 01:15 UTC: `barnhaus-design-os/lib/supabase.ts` anon key swapped from legacy JWT to new-format `sb_publishable_...` key (backup at `.bak-rot20260901`). Likely related to the 401s seen with old keys in MEMORY.md — legacy JWT keys appear rotated/disabled. Verify service-role key + MEMORY.md entries next session.
